# Foundation 模块

Entelechy 引擎的零依赖基础层。

## 关键文件

| 文件 | 用途 |
|------|------|
| `foundation_types.h` | 标量别名、平台宏、断言层级、导出辅助宏 |

## 设计原则

- **无运行时依赖**：本模块仅包含纯编译期宏和类型别名。
- **无链接时依赖**：其他引擎模块（Memory、Math、Core 等）均依赖本模块。

## 规则

- 禁止向本模块添加运行时状态或动态内存分配。
- 保持仅头文件（header-only）；不应需要 `.cpp` 配套文件。
